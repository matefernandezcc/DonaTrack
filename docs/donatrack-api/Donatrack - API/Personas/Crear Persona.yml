info:
  name: Crear Persona
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/donaciones/personas"
  body: json
  auth: inherit

body:json: |-
  {
    "nombre": "Juan Pérez",
    "tipo": "HUMANA"
  }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
