info:
  name: Sugerir Beneficiarios
  type: http
  seq: 4

http:
  method: GET
  url: "{{donacionesUrl}}/api/donaciones/{{donacionId}}/matchmaking"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
