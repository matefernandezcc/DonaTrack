info:
  name: Asignacion Batch
  type: http
  seq: 8

http:
  method: POST
  url: "{{donacionesUrl}}/api/donaciones/asignacion-batch"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
