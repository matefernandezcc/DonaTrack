info:
  name: Obtener Persona
  type: http
  seq: 2

http:
  method: GET
  url: "{{donacionesUrl}}/api/personas/{{personaId}}"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
