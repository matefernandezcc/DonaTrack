info:
  name: Eliminar Persona
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{donacionesUrl}}/api/personas/{{personaId}}"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
