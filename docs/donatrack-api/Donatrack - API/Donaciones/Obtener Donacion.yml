info:
  name: Obtener Donacion
  type: http
  seq: 2

http:
  method: GET
  url: "{{donacionesUrl}}/api/donaciones/{{donacionId}}"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
