info:
  name: Obtener Jobs
  type: http
  seq: 1

http:
  method: GET
  url: "{{logisticaUrl}}/api/jobs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
