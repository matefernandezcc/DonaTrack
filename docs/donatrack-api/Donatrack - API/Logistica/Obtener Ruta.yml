info:
  name: Obtener Ruta
  type: http
  seq: 11

http:
  method: GET
  url: "{{logisticaUrl}}/api/rutas/{{rutaId}}"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
