info:
  name: Ejecutar Planificacion
  type: http
  seq: 8

http:
  method: POST
  url: "{{logisticaUrl}}/api/planificacion/ejecutar"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
