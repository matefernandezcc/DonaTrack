info:
  name: Crear Chofer
  type: http
  seq: 4

http:
  method: POST
  url: "{{logisticaUrl}}/api/choferes"
  body:
    type: json
    data: |-
      {
        "legajo": "CH-003",
        "nombre": "Pedro Gomez"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
