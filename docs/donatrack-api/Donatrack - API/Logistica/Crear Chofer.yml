info:
  name: Crear Chofer
  type: http
  seq: 4

http:
  method: POST
  url: "{{logisticaUrl}}/api/choferes"
  body:
    type: json
    data: |-
      {
        "legajo": "CH-005",
        "nombre": "Mateo Fernandez"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
