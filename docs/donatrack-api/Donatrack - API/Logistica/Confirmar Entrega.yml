info:
  name: Confirmar Entrega
  type: http
  seq: 6

http:
  method: POST
  url: "{{logisticaUrl}}/api/entregas/{{donacionId}}/confirmar"
  body:
    type: json
    data: |-
      {
        "fotos": [
          "http://fotos.donatrack.com/comprobante1.jpg"
        ],
        "patenteCamion": "AA123BB"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
