info:
  name: Iniciar Ruta
  type: http
  seq: 5

http:
  method: POST
  url: "{{logisticaUrl}}/api/rutas/{{rutaId}}/iniciar"
  body:
    type: json
    data: |-
      {
        "legajoChofer": "CH-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
