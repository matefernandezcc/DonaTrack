info:
  name: Listar Rutas
  type: http
  seq: 10

http:
  method: GET
  url: "{{logisticaUrl}}/api/rutas"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
