info:
  name: Reportar Falla
  type: http
  seq: 7

http:
  method: POST
  url: "{{logisticaUrl}}/api/entregas/{{donacionId}}/falla"
  body:
    type: json
    data: |-
      {
        "motivo": "El beneficiario no se encontraba en el domicilio",
        "puedeReplanificarse": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
