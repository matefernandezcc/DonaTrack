info:
  name: Crear Beneficiario
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/donaciones/beneficiarios"
  body: json
  auth: inherit

body:json: |-
  {
    "nombre": "Entidad de Ayuda Mutua"
  }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
